name: handover
description: "End-of-session handover - sync branches, release claims, push changes, report status"
author: "BMad"

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/_bmad/bmm/workflows/4-implementation/handover"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

# Beads integration
beads_workflow: "**/beads-git-workflow.md"

standalone: true
web_bundle: false
